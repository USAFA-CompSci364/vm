name: Install

on:
  pull_request:
  push:
  workflow_dispatch:

jobs:
  install:
    runs-on: ubuntu-18.04

    steps:
      - uses: actions/checkout@v2
      - name: install
        run: |
          docker build .
